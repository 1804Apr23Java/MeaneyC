package daos;

import static org.junit.Assert.assertTrue;

import org.junit.Test;

//import com.revature.project1.dao.EmployeeDao;

import classes.Reimbursement;

public class daoTests {

	//@Test
	/*public void test() {
		//EmployeeDao e = EmployeeDao.getEmployeeDao(getServletContext().getResourceAsStream("connection.properties"));
		//EmployeeDaoImpl btdi = new EmployeeDaoImpl();
		Reimbursement re = new Reimbursement();
		re.employeeId = 5;
		re.imageLocation = "https://imgur.com/gallery/N98BYni".toUpperCase();
		re.state = 2;
		re.resolvingManager = 2;
		boolean itWorked = btdi.submitReimbursementRequest(re);
		assertTrue(itWorked);
	}*/

}
